# add-chart

遵循 `.claude/CLAUDE.md` 的图表与 UI 基线；本 skill 只定义新增图表时的执行步骤。

1. 先确认图表要回答的问题，以及现有 dashboard 数据是否已可复用。
2. 优先把图表数据整形放在 `lib/dashboard.ts` 或相邻 `lib/**` 数据模块。
3. 复用现有仪表盘 card、chart 组件与空状态模式。
4. 保持标签简洁，并优先使用响应式容器。
5. 完成后检查空数据、长标签和窄屏下的表现。
