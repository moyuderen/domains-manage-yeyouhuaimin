# supabase-change

基础数据访问与密钥安全规则以 `.claude/CLAUDE.md` 为准；本 skill 只补充 Supabase 变更时的领域检查点。

- 同步更新受影响的应用类型、映射逻辑和表单数据结构。
- 保持既有嵌套结构与字段命名稳定，避免破坏当前页面与组件消费方式。
- 保存前继续做必要的规范化处理，例如域名字段的 `trim()` 与大小写统一。
- 字段有变更时，回查导入/导出、列表、详情页和表单是否需要同步调整。
- 涉及删除联动时，确认 UI 文案与行为仍符合当前数据级联预期。
