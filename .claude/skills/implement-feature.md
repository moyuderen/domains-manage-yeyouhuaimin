# implement-feature

遵循 `.claude/CLAUDE.md` 的架构、安全、质量与 UI 基线；本 skill 只定义功能实现流程。

1. 先确认需求边界、入口页面和目标交互。
2. 定位相关路由、数据流、Server Action、schema 与可复用组件。
3. 优先沿用现有模式做最小必要改动，避免无关重构。
4. 涉及多层修改时，明确每一处改动的职责归属与影响面。
5. 完成后执行项目要求的验证，并记录仍需人工确认的部分。
