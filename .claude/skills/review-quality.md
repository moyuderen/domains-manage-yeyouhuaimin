# review-quality

遵循 `.claude/CLAUDE.md` 的项目基线；本 skill 只定义收尾审查流程。

检查顺序：
1. 变更范围是否严格符合请求，没有夹带无关改动。
2. 是否优先复用了现有模式、组件、schema 和数据层，而不是新增重复实现。
3. 是否引入了多余 helper、无关重构或不必要的复杂度。
4. 是否存在明显的 UI/交互回归、边界泄露或安全风险。
5. 最后执行并记录需要的验证命令，例如 lint、build 与关键路径手测。
